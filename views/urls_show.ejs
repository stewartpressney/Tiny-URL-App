<!DOCTYPE html>
<html>

<head>
    <style>
    body {
        padding-top: 80px;
        font-family: 'Montserrat', sans-serif;
        color: darkslategrey;
    }

    a {
        font-family: 'Montserrat', sans-serif;
        color: white;
        text-decoration: none;
    }

    .container .jumbotron {
        background-color: #1B3D57;
    }

    .container .jumbotron h1 {
        font-family: 'Arvo', sans-serif;
        color: white;
    }

    .container .jumbotron h2 {
        font-family: 'Montserrat', sans-serif;
        color: white;
    }


    .container .jumbotron h4 {
        font-family: 'Montserrat', sans-serif;
        color: white;
    }

    input,
    button,
    select,
    textarea {
        font-family: 'Montserrat', sans-serif;
    }
    </style>
    <title>Tiny App</title>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
</head>

<body class="container">
    <div class="jumbotron">
        <header>
            <% include ./partials/_header %>
        </header>
        <ul>
            <h2>Stick this in Twitter why dont ya!</h2>
            <h4>

         <% if (user){ %>

            <%= `${shortURL}` %>    :  <%= `${longURL}` %>

        <% }; %>


      </h4>
            <h4 style="margin-top:40px;">Reassign This Tiny URL:</h4>
            <form action="/urls/<%= `${shortURL}` %>" method="POST">
                <input id="longURL" type="text" name="longURL" placeholder="Copy & Paste your URL" style="width: 300px">
                <input type="submit" value="Submit">
            </form>
        </ul>
    </div>
</body>

</html>